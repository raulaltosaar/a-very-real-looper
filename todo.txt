- [MoveTrigger.cs] Test using Vive whether it is necessary to repeat setting 
  trigger's position equal to controller's in DropObj function. Does it work just
  just as well without these two lines?

- Create README file with project intro, description, functionality, how to install 
  (which unity/steamvr versions), images, etc. 
